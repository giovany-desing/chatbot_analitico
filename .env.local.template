# ====================================================================
# .env.local - Configuración para Testing Local con Servicios AWS
# ====================================================================
# Este archivo te permite probar localmente la misma configuración que AWS
# usando PostgreSQL y Redis locales en Docker, pero conectándote a MySQL RDS
#
# USO:
# 1. Copia este archivo: cp .env.local.template .env.local
# 2. Completa los valores reales (elimina los comentarios #TODO)
# 3. Ejecuta: docker-compose -f docker-compose.local.yml up -d
# ====================================================================

# ============ PostgreSQL Local (Docker) ============
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=chatbot_user
POSTGRES_PASSWORD=chatbot_pass_local_123
POSTGRES_DB=chatbot_rag

# ============ MySQL RDS AWS (Externo) ============
# Usa las mismas credenciales que en AWS
MYSQL_HOST=textil.cof2oucystyr.us-east-1.rds.amazonaws.com
MYSQL_PORT=3306
MYSQL_USER=samaca
MYSQL_PASSWORD=Mirringa2024New!
MYSQL_DATABASE=textil

# ============ Redis Local (Docker) ============
REDIS_URL=redis://redis:6379/0
REDIS_TTL=3600

# ============ API Keys (LLM) ============
# TODO: Copia estos valores desde AWS Parameter Store o desde tu .env existente
GROQ_API_KEY=gsk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
OPENAI_API_KEY=  # Opcional
MODAL_API_KEY=ak-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ============ Fine-tuned Model ============
# TODO: Copia desde AWS Parameter Store
FINETUNED_MODEL_ENDPOINT=https://XXXXXXXXXX--viz-model-fastapi-app.modal.run
USAR_FINETUNED_MODEL=true

# ============ LLM Configuration ============
LLM_MODEL=llama-3.1-70b-versatile
LLM_TEMPERATURE=0
LLM_MAX_TOKENS=2000
LLM_TIMEOUT=30

# ============ S3 Buckets (Opcional para local) ============
# Si quieres probar funcionalidad S3 localmente, deja estos valores
# Si no, el sistema funcionará sin S3
S3_TRAINING_BUCKET=chatbot-analitico-prod-training-data
S3_BACKUPS_BUCKET=chatbot-analitico-prod-backups

# ============ App Configuration ============
APP_NAME=ChatbotAnalitico
APP_VERSION=1.0.0
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=DEBUG
AWS_DEFAULT_REGION=us-east-1
